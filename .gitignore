/target
/data/*.db
/data/*.db-shm
/data/*.db-wal
# Benchmark fixtures. `scripts/bench-fixture.py` builds them; they are an
# archive rather than source, and the sanitised one is derived from a live
# database that is not ours to commit.
/data/bench/
.env

# Local-only project notes and audit reports -- not meant for the public repo.
/CLAUDE.md
/track.txt
/AUDITORIA_CLAUDE.md
/AUDITORIA_CODEX.md
/AUDITORIA_REMEDIATION.md
